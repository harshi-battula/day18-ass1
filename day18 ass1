#include<stdio.h>
main()
{
	int n,num,sum=0,rem;
	printf("enter the number");
	scanf("%d",&n);
	num=n;
	while(num!=0)
	{
		rem=num%10;
		sum=sum+rem*rem*rem;
		num=num/10;
		
	}
	if(sum==n)
	printf("is armstrong");
	else
	printf("is not armstrong");
}
